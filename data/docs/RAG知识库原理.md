# RAG 知识库原理

RAG 的完整流程可以分成两步：

## 1. 检索
先把本地文档切成小块，然后把每个小块做向量化，存进向量数据库。
当用户提问时，系统会把问题也转成向量，再从数据库里找最相近的内容。

## 2. 生成
把检索到的相关片段和原始问题一起交给大模型。
模型会基于上下文组织回答，并尽量只回答知识库里存在的信息。

## 这个项目做了什么
- 支持 Markdown、TXT、PDF、DOCX
- 自动切片和重建索引
- 使用 DeepSeek 负责生成
- 使用本地 Sentence Transformer 负责向量检索

